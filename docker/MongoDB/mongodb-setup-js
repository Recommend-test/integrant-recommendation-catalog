let res = [
  'use recommendation',
  db.createUser({
    user: MONGO_USERNAME,
    pwd: 'MONGO_USERNAME',
    roles: [
      {
        role: 'readWrite',
        db: 'recommendation'
      }
    ]
  })
];

printjson(res);